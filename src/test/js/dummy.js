var apa = "chimp";
var notUsed;
console.log("apa is a", apa);
console.log("everything that is written to the console here will end up in the outputproperty var in the ant script");
var failedTests = 0;
phantom.exit(failedTests);